# Search

Project repository.

## Local Run

```powershell
npm start
```

Open `http://127.0.0.1:3000`.

Search results are saved on this PC in:

```text
saved-results/search-results.json
```

Each click on `Сохранить` also creates a separate `.json` file in `saved-results/`.
